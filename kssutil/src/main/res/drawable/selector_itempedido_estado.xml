<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- State when a row is being pressed, but hasn't yet been activated (finger down) -->
    <item android:drawable="@color/btnPresionado" android:state_pressed="true" />

    <!-- Used when the view is "activated". -->
    <item android:state_active="true">
        <shape>
            <solid android:color="@color/btnActivado" />
            <stroke android:width="1dp" android:color="@color/color_Verde" />
            <corners
                android:bottomLeftRadius="5dp"
                android:bottomRightRadius="5dp"
                android:topLeftRadius="5dp"
                android:topRightRadius="5dp" />
        </shape>
    </item>

    <item android:drawable="@color/color_Rojo" android:state_checked="true" />

    <item android:state_selected="true">
        <shape>
            <solid android:color="@color/color_LightSteelBlue" />
            <stroke android:width="2dp" android:color="@color/color_Azul" />
        </shape>
    </item>


    <!-- Default, "just hangin' out" state. -->
    <item android:drawable="@color/color_Smoke" />
</selector>